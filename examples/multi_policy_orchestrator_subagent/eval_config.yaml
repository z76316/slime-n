# AIME-2024 eval for the orchestrator/subagent chain.
# n_samples_per_eval_prompt=1 because eval_fn runs the chain loop itself.

eval:
  datasets:
    - name: aime
      path: /root/aime-2024/aime-2024.jsonl
      rm_type: deepscaler
      n_samples_per_eval_prompt: 1
