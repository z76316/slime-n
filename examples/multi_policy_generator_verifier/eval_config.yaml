# AIME-2024 eval for the generator/verifier chain.
# n_samples_per_eval_prompt=1 because eval_fn runs the 8-chain loop itself.

eval:
  datasets:
    - name: aime
      path: /root/aime-2024/aime-2024.jsonl
      rm_type: deepscaler
      n_samples_per_eval_prompt: 1
